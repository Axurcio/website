---
interface Props {
  href: string;
  label: string;
  variant?: "primary" | "secondary" | "ghost";
  external?: boolean;
  class?: string;
}

const {
  href,
  label,
  variant = "primary",
  external = false,
  class: className = "",
} = Astro.props;

const variants = {
  primary:
    "border border-accent-300 bg-accent-400 text-ink shadow-soft hover:-translate-y-0.5 hover:border-accent-200 hover:bg-accent-300",
  secondary:
    "border border-slate-300 bg-white text-slate-950 hover:-translate-y-0.5 hover:border-brand-400 hover:text-brand-700",
  ghost: "text-slate-700 hover:text-slate-950",
};
---

<a
  href={href}
  target={external ? "_blank" : undefined}
  rel={external ? "noreferrer" : undefined}
  class:list={[
    "inline-flex items-center justify-center gap-2 rounded-md px-5 py-3 text-sm font-extrabold transition duration-200",
    variants[variant],
    className,
  ]}
>
  <span>{label}</span>
  {variant !== "ghost" && <span aria-hidden="true">-&gt;</span>}
</a>
